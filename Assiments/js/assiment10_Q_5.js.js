const poemline = prompt("what is the favourite poem line ");
const linelength = poemline.length;
console.log("the line length is.",linelength);
const svalue =prompt("Enter a starting value");
const evalue = prompt("Enter a ending value");
